<%= turbo_frame_tag @vehicle do %>
  <div class="flex flex-row">
    <div class="w-6 flex justify-center"> <!-- Line -->
      <div class="w-[2px] h-full bg-black"></div>
    </div>
    <ul class="ml-2 mt-1">
      <% @cats.each do |cat| %>
        <%= turbo_frame_tag cat do %>
          <h2 id="accordion-heading-<%= dom_id(cat) %>">
            <button type="button" class="flex items-center font-medium gap-1" data-accordion-target="#accordion-body-<%= dom_id(cat) %>" aria-expanded="false" aria-controls="accordion-body-<%= dom_id(cat) %>">
              <svg data-accordion-inactive-icon class="w-6 h-6" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24">
                <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 7.757v8.486M7.757 12h8.486M21 12a9 9 0 1 1-18 0 9 9 0 0 1 18 0Z"/>
              </svg>
              <svg data-accordion-active-icon class="w-6 h-6" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24">
                <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m15 9-6 6m0-6 6 6m6-3a9 9 0 1 1-18 0 9 9 0 0 1 18 0Z"/>
              </svg>
              <span><%= cat.name %></span>
            </button>
          </h2>

          <div id="accordion-body-<%= dom_id(cat) %>" class="hidden -mt-1" aria-labelledby="accordion-heading-<%= dom_id(cat) %>">
            <%= turbo_frame_tag cat, src: show_types_catalog_index_path(params: { vehicle_id: @vehicle, product_category_id: cat }), loading: "lazy" %>
          </div>
        <% end %>
      <% end %>
    </ul>
  </div>
<% end %>